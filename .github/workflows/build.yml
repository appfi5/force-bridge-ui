name: Build and Push Docker Image

on:
  push:
    branches:
      - feature/ci

env:
  IMAGE_NAME: registry.devops.rivtower.com/app5/magickbase-nervos-force-bridge-ui

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    # 步骤1：检出代码
    - name: Checkout repository
      uses: actions/checkout@v4

    # 步骤2：登录 GitHub Container Registry
    - name: Login to Registry
      uses: docker/login-action@v3
      with:
        registry: registry.devops.rivtower.com
        username: ${{ secrets.APP5_REGISTRY_USERNAME }}  # 优先使用自定义用户名
        password: ${{ secrets.APP5_REGISTRY_PASSWORD }}  # 需要创建名为 GHCR_TOKEN 的 secret


    - name: Build and Push
      run: |
        IMAGETAG=$(git rev-parse --short=7 ${{ github.sha }})-$(TZ=Asia/Shanghai date +'%Y%m%d%H%M')

        # mainnet
        /bin/cp apps/ui/.env-mainnet apps/ui/.env
        docker build -t "${{ env.IMAGE_NAME }}-mainnet:${IMAGETAG}" -f dockerfile-mainnet .
        docker push "${{ env.IMAGE_NAME }}-mainnet:${IMAGETAG}"

        # testnet
        /bin/cp apps/ui/.env-testnet apps/ui/.env
        docker build -t "${{ env.IMAGE_NAME }}-testnet:${IMAGETAG}" -f dockerfile-testnet .
        docker push "${{ env.IMAGE_NAME }}-testnet:${IMAGETAG}"


    # 可选步骤：输出镜像信息
    - name: Show Image Info
      run: |
        echo "Successfully pushed image:"
        echo "${{ env.IMAGE_NAME }}:${{ env.IMAGE_TAG }}"
